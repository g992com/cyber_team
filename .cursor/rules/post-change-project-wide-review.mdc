---
description: 修改完成后进行全工程回顾，防冲突与漏改（工程维护规则）
alwaysApply: true
---

# 修改后全工程回顾（防冲突/漏改）

每次完成修改（新增/删除/重命名/行为变更）后，在结束本轮工作前必须做一次**全工程范围**回顾检查，目标是发现：冲突、漏改、引用未更新、契约不一致、文档/映射未同步。

## 必做清单（按顺序）

- 看本轮改动的差异（包含新增/删除文件），确认改动边界与影响面是否和需求一致。
- 基于本轮改动的关键词做全工程搜索（如：函数/类名、配置 key、路由、API 字段、文件名），确保没有遗留旧引用或未更新的地方。
- 若涉及重命名/移动/删除：检查导入路径、引用链路、注册表/清单类文件（如 manifest、mapping、索引）是否同步更新。
- 若涉及契约（接口/协议/数据结构/配置 schema）：检查调用方与被调用方是否都已更新，示例/fixture/测试数据是否匹配。
- 若涉及文档/规范：检查相关说明、模板、索引是否需要同步更新（特别是指向路径、字段名、步骤）。
- 对本轮改动触达的文件运行诊断（lint/类型检查/格式化/测试中与项目相关的那部分）。发现问题必须在本轮修复或明确记录为后续事项。

## 通过标准

- 全工程搜索不再出现已废弃标识/旧字段/旧路径的残留引用（除非明确标注为兼容代码）。
- 相关联的“代码 + 配置 + 文档 + 映射/清单 + 测试”保持一致，没有漏改导致的断链。
- 无明显冲突（同名不同义、重复定义、契约不一致、引用悬空、死链接）。

## 必须沉淀到备忘（思路/判断/方案）

每次完成修改并通过上述“全工程回顾”后，必须将本次修改的**思路与关键判断、解决方案**整理提炼，追加记录到 `docs/role-skills-design-memo.md`，用于后续追溯与延续设计。

### 需要总结的维度（最小集）

- **背景/触发**：为什么要改（问题、目标、约束、触发上下文）。
- **关键判断（Why）**：做出取舍的核心理由（哪些标准/假设/证据在驱动决策）。
- **备选方案与取舍**：至少列出 1 个备选，以及为何不选。
- **最终方案（What）**：结论与要点（规则/结构/契约/流程的变化点）。
- **影响范围（Where）**：涉及的目录/文件/映射关系；新增/删除/重命名的项。
- **一致性检查（Check）**：本次按哪些关键词做过全工程搜索、检查过哪些清单文件/索引、运行过哪些诊断。
- **遗留与后续**：未解决/待验证/待补齐项（含风险与建议下一步）。

### 追加记录模板（复制即用）

将以下模板追加到 `docs/role-skills-design-memo.md` 末尾（新条目尽量按时间递增；若属于既有主题，可追加到对应小节末尾并标注 superseded/替代关系）。

```markdown
---

## 变更备忘（YYYY-MM-DD）：<一句话标题>

### 背景/触发（Context）
- <问题/目标/约束/触发点>

### 关键判断（Why）
- <核心判断 1：依据/标准/证据>
- <核心判断 2：…>

### 备选方案与取舍（Options）
- 方案 A：<要点>；未选原因：<…>
- 方案 B（采用）：<要点>；采用原因：<…>

### 最终方案（What）
- <结论与关键变化点清单>

### 影响范围（Where）
- 变更文件：
  - 新增：`...`
  - 修改：`...`
  - 删除/重命名：`...`
- 受影响的映射/契约/索引（如有）：<...>

### 一致性检查（Check）
- 全工程搜索关键词：`<kw1>`、`<kw2>`、`<kw3>`
- 已检查的清单/索引/映射：<manifest/mapping/README/模板/…>
- 已运行的诊断：<lint/类型检查/测试/…>

### 遗留与后续（Next）
- <待办/风险/验证点/下一步>
```
